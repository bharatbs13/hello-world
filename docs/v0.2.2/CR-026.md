# CR-026 — RXTA-Owned UAT Environment Script Governance

## Status

Approved for v0.2.2

---

# 1. Objective

Establish RXTA (relix-tool) as the sole owner of all UAT environment lifecycle tooling, ensuring that the Relix runtime repository contains only product code.

RXTA governs the complete lifecycle of UAT infrastructure scripts, including:

- Installation
- Environment setup
- Readiness verification
- Deployment artifact management
- Optional storage cleanup

---

# 2. Scope

RXTA owns the following scripts under:

```text
relix-tool/
└── automation/
    └── deploy/
        └── scripts/
            ├── install_storage.sh
            ├── setup_uat_storage.sh
            ├── verify_storage.sh
            └── cleanup_storage.sh
````

These scripts provision, initialize, verify, and optionally clean UAT storage environments.

The scripts are considered part of the RXTA deployment framework and are not part of the Relix runtime.

## Rationale for Separate Scripts

Installation, setup, verification, and cleanup have distinct safety profiles and execution semantics.

| Operation    | Responsibility                        |
| ------------ | ------------------------------------- |
| Installation | Modifies OS packages                  |
| Setup        | Creates databases, users, and schemas |
| Verification | Must be read-only                     |
| Cleanup      | Can destroy UAT state                 |

Separating these concerns ensures each operation can be:

* Governed independently
* Invoked selectively

---

# 3. Required Architecture

```text
                         RXTA (relix-tool)

┌──────────────────────────────────────────────────────────┐
│ Owns UAT environment scripts                            │
│ Deploys scripts to relix-vm                             │
│ Executes install/setup/verify/cleanup                   │
│ Maintains deployment logs                               │
│ Removes deployment artifacts                            │
└──────────────────────────────────────────────────────────┘
                         │
                         ▼

                 relix-vm (Target Environment)

┌──────────────────────────────────────────────────────────┐
│ Temporary deployment artifacts                           │
│ UAT databases                                            │
│ Backend services                                         │
│ Verification utilities                                   │
│ UAT execution workspace                                  │
└──────────────────────────────────────────────────────────┘
                         ▲
                         │

                 Relix Runtime Repository

┌──────────────────────────────────────────────────────────┐
│ Runtime code                                             │
│ Application logic                                        │
│ Storage adapters                                         │
│ Configuration                                            │
│ Database migrations                                      │
│                                                          │
│ No ownership of UAT infrastructure scripts               │
└──────────────────────────────────────────────────────────┘
```

---

# 4. Ownership Rules

## RXTA Owns

* UAT environment scripts
* Deployment to target environments
* Installation scripts
* Environment setup scripts
* Readiness verification scripts
* Cleanup scripts
* Deployment logging
* Deployment artifact lifecycle

## Relix Runtime Owns

* Runtime code
* Application logic
* Storage access
* Migrations
* Runtime configuration
* Business functionality

The Relix runtime shall not depend upon RXTA UAT scripts.

---

# 5. Script Execution Modes

Lifecycle scripts shall be runnable in two modes:

* Manual Mode
* Orchestrated Mode

---

## 5.1 Manual Mode

Developers or operators execute scripts directly on the target environment.

Example usage:

```bash
./automation/deploy/scripts/install_storage.sh --backend postgres
./automation/deploy/scripts/setup_uat_storage.sh --backend postgres
./automation/deploy/scripts/verify_storage.sh --backend postgres
./automation/deploy/scripts/cleanup_storage.sh --backend postgres
```

---

## 5.2 Orchestrated Mode

`relix-deploy` executes scripts as part of automated deployment.

Example usage:

```bash
relix-deploy install-storage --backend postgres
relix-deploy setup-storage --backend postgres
relix-deploy verify-storage --backend postgres
relix-deploy cleanup-storage --backend postgres
```

---

# 6. State Ownership Rule

Manual script execution is stateless.

Orchestrated execution is stateful.

The lifecycle scripts shall not own deployment state.

* When executed manually, scripts perform only the requested operation and emit deterministic output.
* When executed through RXTA orchestration, lifecycle state, retries, execution history, deployment identifiers, and artifact tracking are owned by the orchestrator.

| Mode         | State Owner  | Script Responsibility                                       |
| ------------ | ------------ | ----------------------------------------------------------- |
| Manual       | None         | Execute one operation, emit result, exit                    |
| Orchestrated | relix-deploy | Track lifecycle, retries, logs, run IDs, artifact inventory |

---

# 7. Mode Compatibility

Scripts shall behave identically in both modes:

* No interactive prompts
* Deterministic exit codes
* Human-readable console output
* Machine-readable output (where applicable)
* Timeout support
* SKIPPED for unsupported backends
* FAILED with diagnostic reason for failures

The orchestrator remains the preferred production entry point, while manual execution supports:

* Development
* Debugging
* Ad-hoc operations

# 8. Script Requirements

Scripts shall:

- Be versioned with RXTA
- Be deterministic
- Support repeated execution where practical
- Support backend selection (`--backend <backend>` or `--all`)
- Report unsupported backends as SKIPPED
- Return non-zero exit codes only for genuine failures
- Generate deterministic execution logs
- Support readiness verification after installation and setup

Verification shall be read-only and shall never modify backend state.

---

# 9. Installation Requirements

Installation scripts shall execute deterministically without requiring manual user interaction.

Installation shall:

- Execute in unattended (non-interactive) mode
- Complete successfully or terminate with a deterministic failure
- Never pause awaiting console input
- Never rely on manual confirmation during automated deployment
- Return an appropriate exit status upon completion

Platform-specific mechanisms (for example, non-interactive package manager modes) may be used to satisfy this requirement.

---

# 10. Timeout Governance

Installation, setup, and verification operations shall support configurable execution timeouts.

Operations exceeding the configured timeout shall terminate with FAILED and include a timeout diagnostic message.

Timeout values shall be configurable through RXTA deployment policy.

Upon timeout, the deployment framework shall:

- Terminate the underlying installation process where practical
- Record timeout diagnostics
- Report the operation as FAILED

---

# 11. Retry Governance

Deployment tooling may retry recoverable installation and verification failures according to RXTA deployment policy.

Retry behavior shall be:

- Deterministic
- Auditable

Retry attempts shall be recorded in deployment logs.

Retries shall not be attempted for:

- Deterministic configuration errors
- Interactive package-manager prompts

unless the deployment policy explicitly provides an automated recovery action.

---

# 12. Package Manager Behaviour

When installation requires operating-system package management, installation scripts shall:

- Invoke the package manager in non-interactive mode
- Detect package installation failures
- Detect incomplete or pending package configuration
- Detect package-manager lock or transaction failures
- Terminate cleanly if installation cannot proceed automatically

Installation scripts shall not remain blocked waiting for:

- Confirmation prompts
- Configuration dialogs
- License acceptance dialogs
- Terminal input
- Package-manager questions

Any such condition shall be treated as an installation failure.

---

# 13. Installation Verification

Upon completion of installation, verification shall determine whether installation completed successfully.

Verification shall distinguish between:

| State |
|---|
| Installation completed successfully |
| Installation failed |
| Installation incomplete |
| Installation pending manual configuration |

Verification diagnostics shall include the reason when installation is not successful.

Example diagnostic messages include:

- Package manager returned non-zero exit status
- Package installation requires manual configuration
- Package manager transaction incomplete
- Package manager lock unavailable
- Required package not installed

---

# 14. Verification Contract

Verification validates that a backend is correctly prepared for UAT execution.

Verification shall execute mandatory verification checks:

- Installation completion
- Service availability
- Setup completion

Verification may execute backend-specific optional checks including:

- Authentication
- Connectivity
- Schema validation
- Test object availability
- Configuration validation

Verification shall execute without requiring manual SSH inspection of the target environment.

---

# 15. Verification Status

Each backend shall report individual verification results.

Verification results are organized by check category.

## Installation

| Status |
|---|
| INSTALLED |
| NOT_INSTALLED |

## Service

| Status |
|---|
| SERVICE_READY |
| SERVICE_NOT_READY |

## Setup

| Status |
|---|
| SETUP_READY |
| SETUP_NOT_READY |

## Execution

| Status |
|---|
| PASSED |
| FAILED |
| SKIPPED |
| NOT_APPLICABLE |

FAILED and SKIPPED shall include a diagnostic reason.

# 16. Verification Output

Verification shall produce:

- Human-readable console output
- Machine-readable structured output suitable for deployment automation

The structured output shall contain, at minimum:

| Field |
|---|
| backend |
| installation status |
| service status |
| setup status |
| overall readiness |
| diagnostic message (if applicable) |

The machine-readable output shall include a schema version identifier.

Consumers shall ignore unknown fields to preserve forward compatibility.

---

# 17. Deployment Logging

Deployment logs shall record:

| Field |
|---|
| deployment identifier |
| execution identifier |
| host identifier |
| deployed RXTA version |
| deployed script version |
| backend |
| executed operation |
| execution status |
| execution result |
| timestamp |

Deployment logs shall allow operators to determine which operation failed without requiring manual inspection of the deployment host.

---

# 18. Deployment Lifecycle

The orchestrator may invoke lifecycle scripts in the following phases:

- install
- setup
- verify
- cleanup (only when explicitly requested)

Orchestration order and coordination are defined in CR-028.

---

# 19. Delete

`relix-deploy delete` removes only RXTA deployment artifacts from the target environment.

Deployment artifacts include:

- Files copied to the target environment during deployment
- Temporary deployment files
- Temporary execution directories

Typical deployment locations include:

```text
/opt/relix-workspace/uat-artifacts
/tmp/relix-uat-*
Canonical copies remain inside the RXTA repository.
Database objects shall not be removed by default.

20. Storage Cleanup
Storage cleanup is an explicit operation.
Examples:
relix-deploy cleanup-storage --backend postgres
or
relix-deploy delete --cleanup-storage
Cleanup may include:
* Dropping UAT databases
* Removing test schemas
* Removing test users
* Resetting storage state
Storage cleanup shall:
* Require explicit invocation
* Be auditable
* Never execute automatically during deployment deletion

21. Acceptance Criteria
ID	Acceptance Criteria
AC-001	UAT environment scripts exist under relix-tool/automation/deploy/scripts
AC-002	relix-deploy can deploy and execute all lifecycle scripts on the target environment
AC-003	The Relix runtime repository contains no UAT infrastructure scripts
AC-004	Scripts are directly executable in manual mode
AC-005	Scripts are callable through relix-deploy in orchestrated mode
AC-006	Scripts behave identically in both modes (no interactive prompts, deterministic exit codes, human-readable output, machine-readable output where applicable)
AC-007	Scripts do not own deployment state in manual mode
AC-008	The orchestrator owns lifecycle state, retries, logs, run IDs, and artifact inventory in orchestrated mode
AC-009	Setup is safe for repeated execution where practical
AC-010	Unsupported backends report SKIPPED
AC-011	Verification is read-only
AC-012	Verification executes without manual SSH inspection
AC-013	Verification produces both human-readable and machine-readable output
AC-014	Verification output includes a schema version identifier
AC-015	Verification output consumers ignore unknown fields
AC-016	Deployment logs capture deployment identifier, execution identifier, host identifier, version, backend, operation, status, result, and timestamp
AC-017	relix-deploy delete removes only deployment artifacts by default
AC-018	Canonical script copies remain within the RXTA repository
AC-019	Storage cleanup requires explicit user invocation
AC-020	Deployment artifact cleanup and storage cleanup are independently executable
AC-021	Installation executes without requiring manual operator interaction
AC-022	Installation never blocks awaiting package-manager prompts
AC-023	Package-manager prompts are reported as installation failures with diagnostic information
AC-024	Incomplete or pending package configuration is detected during verification
AC-025	Automated deployment either completes successfully or fails deterministically without hanging
AC-026	Installation, setup, and verification operations support configurable execution timeouts
AC-027	Timeout diagnostics are included when operations exceed configured timeout values
AC-028	Upon timeout, the deployment framework terminates the underlying process where practical
AC-029	Retry behavior, where implemented, is deterministic and auditable
AC-030	Retries are not attempted for deterministic configuration errors or interactive package-manager prompts without an explicit automated recovery actio
CR-026 conversion complete. Only Markdown restructuring was applied:
- headers
- tables
- fenced blocks
- list normalization

No requirement, ownership, lifecycle, or acceptance-criteria semantics changed.


