max : 91

fresh_sync_commit.sh          = additive/overwrite sync, no delete
fresh_mirror_commit.sh        = full mirror, deletes stale files
smart_partial_sync.sh         = selective files/folders sync
