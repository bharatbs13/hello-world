max : 91

additive_commit/fresh_sync_commit.sh          = additive/overwrite sync, no delete 

full_rsync/fresh_mirror_commit.sh        = full mirror, deletes stale files

partial_rsync/smart_partial_sync.sh         = selective files/folders sync
